package usecases_test

import (
	"testing"
)

func TestWriteLocation_Nominal(t *testing.T) {
	/*assert := assert.New(t)

	controller := gomock.NewController(t)
	defer controller.Finish()

	repo := mock_repo.NewMockLocationWriter(controller)
	repo.EXPECT().WriteLocation(location).Return(nil)

	uc := usecases.NewReceiveLocationUC(repo)
	err := uc.WriteLocation(location)

	assert.Nil(err)*/
}
